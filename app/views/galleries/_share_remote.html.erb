"share_remote".html.erb
<%= simple_form_for gallery, remote: true do |f| %>

  <%= f.input  :recipient_name %>
  <%= f.input  :recipient_email %>
  <%= f.button :submit, class: 'btn btn-primary' %>

<% end %>
